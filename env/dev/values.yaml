image:
  repository: ghcr.io/your-org/kargo-demo
  tag: "latest"  # Kargo updates this

env:
  APP_ENV: dev
  APP_VERSION: latest

replicaCount: 1

ingress:
  enabled: true
  host: kargo-demo-dev.example.com
