image:
  repository: ghcr.io/your-org/kargo-demo
  tag: "latest"  # Kargo updates this

env:
  APP_ENV: prod
  APP_VERSION: latest

replicaCount: 2

ingress:
  enabled: true
  host: kargo-demo.example.com

rollout:
  enabled: true
  strategy: blueGreen
