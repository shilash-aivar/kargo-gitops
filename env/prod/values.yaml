image:
  repository: 18bit048/kargo-repo
  tag: 1.0.2

env:
  APP_ENV: prod
  APP_VERSION: latest

replicaCount: 2

ingress:
  enabled: false
  # host: kargo-demo.example.com

# rollout:
#   enabled: true
#   strategy: blueGreen
